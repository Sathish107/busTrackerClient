const SearchInstance=({start,destination})=>{
    return(
        <div>
            <p>{start}</p>
            <p>to</p>
            <p>{destination}</p>
        </div>
    )
}

export default SearchInstance